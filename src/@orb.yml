version: 2.1

description: >
  Persists environment variables across jobs. Supports custom workspace locations to better handle special cases.

# This information will be displayed in the orb registry and is not mandatory.
display:
  source_url: "https://github.com/opub/persist-env-orb"
